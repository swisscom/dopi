name: 'test_plan_with_invalid_role'

infrastructures:
  baremetal: {}

nodes:
  'linux01.example.com':
    type: 'baremetal'

steps:

  - name: 'Wait 1'
    nodes:
      - 'linux01.example.com'
    roles:
      - 'nonexistingrole'
    max_in_flight: 2
    command:
      plugin: 'custom'
      exec: 'echo'
      arguments: 'Hello World'

