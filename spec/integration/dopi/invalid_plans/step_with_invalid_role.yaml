name: 'test_plan_with_invalid_role'

infrastructures:
  'test':
    type: 'baremetal'

nodes:
  'linux01.example.com':
    infrastructure: 'test'

steps:

  - name: 'Wait 1'
    nodes:
      - 'linux01.example.com'
    roles:
      - 'nonexistingrole'
    max_in_flight: 2
    command:
      plugin: 'custom'
      exec: 'echo'
      arguments: 'Hello World'

