name: 'test_plan_with_invalid_node'

infrastructures:
  'test':
    type: 'baremetal'

nodes:
  'linux01.example.com':
    infrastructure: 'test'

steps:

  - name: 'Wait 1'
    nodes:
      - 'linux01.example.com'
      - 'linux02.example.com'
    max_in_flight: 2
    command:
      plugin: 'custom'
      exec: 'echo'
      arguments: 'Hello World'

