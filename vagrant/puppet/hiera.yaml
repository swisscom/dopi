:backends:
    - dop
    - yaml
:hierarchy:
    - hosts/%{::clientcert}
    - roles/%{::my_role}
    - defaults
:yaml:
    :datadir: /vagrant/vagrant/puppet/hieradata
:dop:
    :plan_dir: /var/lib/dop/plans
